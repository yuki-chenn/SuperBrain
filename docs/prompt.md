将管理员界面所有的打开形式改为页签形式，可以在页签之间随意切换，仪表盘、用户管理、游戏管理（每个游戏单独页签）、题库管理（每个游戏单独页签）、排行榜、挑战记录、审计日志。都作为页签形式。支持随意切换、以及关闭。

=====================================

存在问题：
1. 开始挑战时，会自动往挑战记录数据库中写入两条信息。
2. 绝对指令中退出按钮改为放弃挑战，并且弹出的对话框是透明的，点击后没有任何反应。
3. 管理员界面当点击刷新后，之前所有的页签都消失了。我不希望刷新按钮会重置页签.

=====================================

存在问题：
1. admin挑战记录中sliding-puzzle游戏的题目没有显示
2. 修改了六维属性后http://localhost:5173/games界面的卡片中的六位雷达图没有变。

新增需求：
admin界面增加一个菜单栏“所有数据库”，这个菜单栏会展示目前所有的数据库信息，包括数据库每个字段以及对应的数据。


=====================================

你需要先阅读 @docs/Oerview- 以及项目代码了解整个项目机构。
我现在需要你对整个项目的部分框架设计做重构，主要分为3个方面：
1. 数据库设计重构，你需要阅读 @docs/8- ，根据文档的设计对整个项目做重构。
2. 游戏流程设计，你需要阅读 @docs/7- ，根据文档的设计对整个项目做重构。
3. 并发架构设计， 你需要阅读 @docs/9- ，根据文档的设计对整个项目做重构。

注意：这三点之间可能存在耦合的请情况，你需要根据文档中的内容总体对整个项目的架构做重构。如果文档中出现冲突的地方你需要询问过我，由我来做决策。你的整体代码要符合设计规范和设计模式、要满足高可靠、高可用，但是不要过度设计。

最后我需要你将修改后的项目整体设计写入 Overview- 开头的文档中，方便我后续接入新的游戏所需要使用的提示词。你可以修改现有的Overview，也可以增加新的Overview文件，最好是每个独立的模块一个overview文件。后续如果我需要新增游戏的需求你也需要帮我写好新增游戏需要的规则，写入 rule- 开头的文件，同样也是每个独立的模块一个 rule- 文件。
